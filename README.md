# 106a_final_project
for 106a_final_project
using tello drone tracking the Aruco code motion (transition, rotation, etc.)

pip3 install opencv-contrib-python numpy

sudo apt update
sudo apt install ros-humble-rclpy ros-humble-sensor-msgs ros-humble-geometry-msgs ros-humble-cv-bridge

